import React, { useState } from "react";
import { useNavigate } from "react-router-dom";
import { createPageUrl } from "@/utils";
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Badge } from "@/components/ui/badge";
import { 
  CreditCard, Plane, Car, Baby, Heart, Users, Home, 
  Building, Receipt, Vote, Search, ArrowRight, Clock, DollarSign 
} from "lucide-react";

const SERVICES = [
  {
    id: "id_card",
    name: "ID Card Application",
    description: "Apply for a new South African ID card or smart card replacement",
    icon: CreditCard,
    category: "Identity Documents",
    fee: "R140",
    processing_time: "6-8 weeks",
    color: "from-blue-500 to-blue-600",
  },
  {
    id: "passport",
    name: "Passport Application",
    description: "Apply for or renew your South African passport",
    icon: Plane,
    category: "Identity Documents",
    fee: "R400",
    processing_time: "3-6 weeks",
    color: "from-purple-500 to-purple-600",
  },
  {
    id: "drivers_license",
    name: "Driver's License",
    description: "Apply for a learner's or driver's license",
    icon: Car,
    category: "Transport Services",
    fee: "R250",
    processing_time: "4-6 weeks",
    color: "from-red-500 to-red-600",
  },
  {
    id: "birth_certificate",
    name: "Birth Certificate",
    description: "Register a birth or request a birth certificate copy",
    icon: Baby,
    category: "Civil Registration",
    fee: "Free",
    processing_time: "2-3 weeks",
    color: "from-pink-500 to-pink-600",
  },
  {
    id: "marriage_certificate",
    name: "Marriage Certificate",
    description: "Register marriage or request certificate copy",
    icon: Heart,
    category: "Civil Registration",
    fee: "Free",
    processing_time: "2-3 weeks",
    color: "from-rose-500 to-rose-600",
  },
  {
    id: "death_certificate",
    name: "Death Certificate",
    description: "Register death or request certificate copy",
    icon: Users,
    category: "Civil Registration",
    fee: "Free",
    processing_time: "2-3 weeks",
    color: "from-gray-500 to-gray-600",
  },
  {
    id: "sassa_grant",
    name: "SASSA Grant Application",
    description: "Apply for social assistance grants (Old Age, Disability, Child Support)",
    icon: Users,
    category: "Social Services",
    fee: "Free",
    processing_time: "3 months",
    color: "from-green-500 to-green-600",
  },
  {
    id: "nsfas",
    name: "NSFAS Application",
    description: "Apply for National Student Financial Aid Scheme funding",
    icon: Users,
    category: "Social Services",
    fee: "Free",
    processing_time: "4-6 weeks",
    color: "from-indigo-500 to-indigo-600",
  },
  {
    id: "rdp_housing",
    name: "RDP Housing Application",
    description: "Apply for government subsidized housing",
    icon: Home,
    category: "Housing Services",
    fee: "Free",
    processing_time: "Varies",
    color: "from-orange-500 to-orange-600",
  },
  {
    id: "business_registration",
    name: "Business Registration",
    description: "Register a new business or company with CIPC",
    icon: Building,
    category: "Business Services",
    fee: "From R175",
    processing_time: "5-10 days",
    color: "from-cyan-500 to-cyan-600",
  },
  {
    id: "tax_registration",
    name: "Tax Registration",
    description: "Register for tax with SARS or update tax information",
    icon: Receipt,
    category: "Tax Services",
    fee: "Free",
    processing_time: "2-3 weeks",
    color: "from-amber-500 to-amber-600",
  },
  {
    id: "voter_registration",
    name: "Voter Registration",
    description: "Register to vote or update voter information",
    icon: Vote,
    category: "Electoral Services",
    fee: "Free",
    processing_time: "Immediate",
    color: "from-teal-500 to-teal-600",
  },
];

const CATEGORIES = [
  "All Services",
  "Identity Documents",
  "Civil Registration",
  "Social Services",
  "Housing Services",
  "Business Services",
  "Tax Services",
  "Transport Services",
  "Electoral Services"
];

export default function Services() {
  const navigate = useNavigate();
  const [searchQuery, setSearchQuery] = useState("");
  const [selectedCategory, setSelectedCategory] = useState("All Services");

  const filteredServices = SERVICES.filter(service => {
    const matchesSearch = service.name.toLowerCase().includes(searchQuery.toLowerCase()) ||
                         service.description.toLowerCase().includes(searchQuery.toLowerCase());
    const matchesCategory = selectedCategory === "All Services" || service.category === selectedCategory;
    return matchesSearch && matchesCategory;
  });

  const handleApply = (serviceId) => {
    navigate(createPageUrl("ApplyService") + `?type=${serviceId}`);
  };

  return (
    <div className="p-4 md:p-8 bg-[#FAFAF9] min-h-screen">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <h1 className="text-3xl md:text-4xl font-bold text-gray-900 mb-2">Government Services</h1>
          <p className="text-gray-600">Apply for various government services online</p>
        </div>

        {/* Search and Filter */}
        <div className="mb-8 space-y-4">
          <div className="relative">
            <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400" />
            <Input
              type="text"
              placeholder="Search for a service..."
              value={searchQuery}
              onChange={(e) => setSearchQuery(e.target.value)}
              className="pl-10 h-12 text-base"
            />
          </div>

          <div className="flex flex-wrap gap-2">
            {CATEGORIES.map((category) => (
              <Badge
                key={category}
                variant={selectedCategory === category ? "default" : "outline"}
                className={`px-4 py-2 cursor-pointer transition-all ${
                  selectedCategory === category
                    ? 'bg-[#007749] hover:bg-[#005a38] text-white'
                    : 'hover:bg-gray-100'
                }`}
                onClick={() => setSelectedCategory(category)}
              >
                {category}
              </Badge>
            ))}
          </div>
        </div>

        {/* Services Grid */}
        {filteredServices.length === 0 ? (
          <div className="text-center py-16">
            <Search className="w-16 h-16 text-gray-300 mx-auto mb-4" />
            <h3 className="text-xl font-semibold text-gray-900 mb-2">No services found</h3>
            <p className="text-gray-600">Try adjusting your search or filter criteria</p>
          </div>
        ) : (
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
            {filteredServices.map((service) => {
              const Icon = service.icon;
              return (
                <Card 
                  key={service.id}
                  className="group hover:shadow-xl transition-all duration-300 border-none overflow-hidden"
                >
                  <CardHeader className={`bg-gradient-to-br ${service.color} text-white pb-6`}>
                    <div className="flex items-start justify-between">
                      <div className="w-14 h-14 rounded-2xl bg-white/20 backdrop-blur-sm flex items-center justify-center group-hover:scale-110 transition-transform">
                        <Icon className="w-7 h-7" />
                      </div>
                      <Badge className="bg-white/20 backdrop-blur-sm text-white border-0">
                        {service.category}
                      </Badge>
                    </div>
 
